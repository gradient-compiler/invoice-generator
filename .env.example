# Required: 32-byte hex string for encrypting sensitive data at rest (PII, SMTP credentials, bank info)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# Override default database directory (default: ./data/)
# INVOICE_DB_DIR=/path/to/persistent/data
